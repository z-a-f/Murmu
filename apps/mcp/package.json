{
  "name": "@nonomessage/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "nonomessage-mcp": "./dist/server.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/server.ts",
    "typecheck": "tsc -b tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@nonomessage/client": "workspace:*",
    "@nonomessage/protocol": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
